<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OrizelSchool" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/Orizel</item>
    </style>

    <style name="Theme.OrizelSchool" parent="Base.Theme.OrizelSchool" />

    <style name="ToolbarTitleTextAppearance">
        <item name="android:textSize">21sp</item> <!-- Adjust text size as needed -->
        <item name="android:textStyle">bold</item> <!-- Make the text bold -->
        <item name="android:gravity">bottom</item> <!-- Center text horizontally and vertically -->
    </style>

</resources>